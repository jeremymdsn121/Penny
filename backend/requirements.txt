# Core API
fastapi
uvicorn[standard]

# Settings / validation
pydantic
pydantic-settings
email-validator

# HTTP client for Supabase Auth + PostgREST (and later Rentcast)
httpx

# File uploads (FastAPI multipart)
python-multipart

# Contract PDF text extraction
pymupdf

# AI field extraction
anthropic

# .env loading
python-dotenv

# WhatsApp / SMS messaging
twilio

# Voice memo transcription
openai

# Transactional email
sendgrid

# DOCX text extraction (knowledge-base style docs)
python-docx

# IANA timezone database (Windows has no system tzdata) — scheduling slot math
tzdata
